{
  "name": "@family/auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Server-side authentication and authorization primitives (spec §18)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --emitDeclarationOnly --outDir dist || true",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run",
    "lint": "eslint src"
  },
  "dependencies": {
    "@family/contracts": "workspace:*",
    "aws-jwt-verify": "5.1.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@family/eslint-config": "workspace:*",
    "@family/tsconfig": "workspace:*",
    "@types/node": "^24.9.0",
    "typescript": "6.0.3",
    "vitest": "4.1.11"
  }
}
