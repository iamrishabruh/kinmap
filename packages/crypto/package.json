{
  "name": "@family/crypto",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Application-level envelope encryption for coordinates (spec §20)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --emitDeclarationOnly --outDir dist || true",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run",
    "lint": "eslint src"
  },
  "dependencies": {
    "@aws-sdk/client-kms": "3.1101.0",
    "@family/contracts": "workspace:*",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@family/eslint-config": "workspace:*",
    "@family/tsconfig": "workspace:*",
    "@types/node": "^24.9.0",
    "typescript": "6.0.3",
    "vitest": "4.1.10"
  }
}
